# Ramesh Content Engine

Vite + React + TypeScript content generator (Gold edition).  
Features:
- Gold UI theme, dark/light toggle
- AI status indicator
- Disabled Generate button when AI quota is exhausted
- Error boundary + friendly fallback

## Run locally
```bash
npm install
npm run dev
